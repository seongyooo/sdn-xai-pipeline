# 세션 작업 기록 — 2026-07-20

## 개요

이 문서는 2026-07-20 세션에서 `sdn-xai-pipeline`에 진행한 모든 작업과 `sdn_intent-framework/experiments/e1` 분석 내용을 기록한다.

---

## 1. UI 개선: Intent 예시 칩 확장

### 배경
기존 intent 예시 칩이 forwarding, QoS, security(block) 3종류만 있었다. SFC와 reroute 인텐트도 예시로 제공해야 한다.

### 변경 내용

**`static/index.html`** — 칩 4개 추가 (총 11개로 확장)

| 추가 칩 | intent 내용 |
|---|---|
| SFC | `Steer HTTP traffic from 10.0.0.1 to 10.0.0.4 through IDS at port 9, then forward out port 2 on switch 2` |
| Reroute | `Reroute traffic from 10.0.0.2 to 10.0.0.3 via switch 3 avoiding switch 2 on switch 1` |
| Compound (Allow HTTP, Block SSH) | `Allow HTTP from 10.0.0.1 to 10.0.0.2 via port 2 on switch 1, but block SSH from 10.0.0.1 to 10.0.0.2 on switch 1` |
| Compound (Block ICMP + QoS) | `Block ICMP from 10.0.0.1 to 10.0.0.3 on switch 1 and apply QoS for video streaming from 10.0.0.2 to 10.0.0.4 on switch 1` |
| Compound (Multi-switch rules) | `Forward HTTP from 10.0.0.1 to 10.0.0.3 via port 2 on switch 1, and block all traffic from 10.0.0.2 to 10.0.0.4 on switch 2` |

**`static/app.js`** — `updateExampleChips()` 함수에도 동일 칩 추가 (커스텀 토폴로지 로드 시 동적 재생성)

---

## 2. UI 버그 수정: 토폴로지 프리셋 드롭다운 클리핑

### 증상
"⊞ Presets" 버튼 클릭 시 드롭다운 메뉴가 잘리거나 아예 안 보였다. 항목이 6~7개만 나오다 멈췄다.

### 원인
`#topology-panel`에 `overflow-y: auto`가 걸려 있어서 `position: absolute`인 `.preset-menu`가 부모 컨테이너 경계에서 잘렸다.

### 수정

**`static/style.css`**
```css
/* 변경 전 */
.preset-menu { position: absolute; ... }

/* 변경 후 */
.preset-menu {
  position: fixed;
  z-index: 9999;
  max-height: calc(100vh - 80px);
  overflow-y: auto;
}
```

**`static/app.js`** — `initTopoPresets()`에서 `getBoundingClientRect()`로 동적 위치 계산
- 버튼 아래에 메뉴 공간이 충분하면 아래로 열림
- 공간이 부족하면 버튼 위로 열림 (above logic)

---

## 3. UI 기능: 인텐트 칩 클릭 시 한국어 번역 표시

### 배경
각 intent 예시가 영어로만 설명되어 있어 의미를 바로 파악하기 어렵다. 칩 클릭 시 해당 intent가 네트워크에서 무슨 의미인지 한국어로 보여주는 기능 추가.

### 구현

**`static/index.html`**
- 각 칩에 `data-kr="..."` 속성 추가
- `<div id="intent-translation"></div>` 요소를 chips 아래에 추가

**`static/style.css`**
```css
#intent-translation {
  display: none;
  margin-top: 10px;
  padding: 10px 14px;
  background: #0d1424;
  border: 1px solid #1e3a5f;
  border-left: 3px solid #3b82f6;
  border-radius: 8px;
  font-size: 12px;
  color: #93c5fd;
}
#intent-translation.visible { display: block; }
```

**`static/app.js`**
- `fillIntent(text, kr)`: 선택적 `kr` 파라미터 추가
- `showIntentTranslation(kr)` / `hideIntentTranslation()` 함수 추가
- textarea 수동 입력 시 번역 박스 자동 숨김

---

## 4. 버그 수정: Digital Twin reroute/qos/sfc 검증 오류

### 증상
reroute 인텐트를 실행했을 때 Digital Twin 검증이 "reach" 여부를 잘못 판단했다.

### 원인 (2가지)

1. **`action` 탐지 오류**: `twin_verifier.py`가 flow rule 구조(OUTPUT 명령 유무)로 action을 역추론했는데, reroute가 forward와 동일한 구조라 구분 불가
2. **`expect_reach` 오류**: `expect_reach = (action == "forward")`로 하드코딩되어 reroute/qos/sfc는 항상 false로 처리됨

### 수정

**`pipeline/stage2_flowrule/compiler.py`** — 모든 액션 반환에 `intent_action` 키 추가
```python
# block
return {"intent_action": "block", "flows": [...]}
# forward
return {"intent_action": "forward", "flows": [...]}
# qos, reroute, sfc 동일
```

**`pipeline/stage4_twin/twin_verifier.py`**
```python
# 변경 전
has_output = any(i.get("type") == "OUTPUT" for i in instructions)
action = "forward" if has_output else "block"
expect_reach = (action == "forward")

# 변경 후
if "intent_action" in flowrule:
    action = flowrule["intent_action"]
else:
    ...  # fallback
expect_reach = (action != "block")  # block만 차단, 나머지는 모두 도달 기대
```

---

## 5. 주요 신기능: 복합 인텐트(Compound Intent) 지원

### 배경
기존 파이프라인은 하나의 자연어 인텐트 → 하나의 FlowRule만 처리했다. 실제 네트워크 운영에서는 "HTTP는 허용하고 SSH는 차단하라" 같이 여러 정책을 동시에 요청하는 경우가 많다.

### 아키텍처 변경

복합 인텐트는 후방 호환을 유지하면서 병렬 경로로 처리된다:

```
자연어 인텐트
    ↓ Stage 1 (LLM)
    ├── 단일 룰 → IntentPrediction(program=IntentIR)  [기존 경로]
    └── 복합 룰 → IntentPrediction(compound=CompoundIntentIR)  [신규 경로]
        ↓ Stage 2
    FlowRule { intent_action: "compound", sub_rules: [...], flows: [...] }
        ↓ Stage 3
    Intra-conflict detection (복합 룰 간 충돌)
        ↓ Stage 4
    Digital Twin (각 sub-rule별 검증)
```

### 파일별 변경 내용

#### `models/intent_ir.py`
- `IntentPrediction`에 `compound: Optional[CompoundIntentIR]` 필드 추가
- `CompoundIntentIR` 클래스 신규 추가:
  ```python
  class CompoundIntentIR(BaseModel):
      rules: list[IntentIR]
      description: str = ""
  ```

#### `pipeline/stage1_intent/intent_parser.py`
- `SYSTEM_PROMPT` 전면 재작성: `{"rules": [...], "description": "..."}` 통일 포맷으로 출력
- 복합 인텐트 예시 추가 (Allow HTTP + Block SSH 등)
- `parse()`: `rules` 배열 유무로 단일/복합 라우팅
- `_parse_single()`: 구형 단일 JSON 포맷 하위 호환 처리
- `_parse_rules()`: 각 rule에 토폴로지 그라운딩 적용, len≥2이면 `CompoundIntentIR` 반환

#### `pipeline/stage2_flowrule/compiler.py`
- `compile_compound(compound: CompoundIntentIR) -> dict` 함수 신규 추가:
  ```python
  {
    "intent_action": "compound",
    "description": "...",
    "sub_rules": [각 sub-rule 결과],
    "flows": [모든 flow 합산]
  }
  ```

#### `pipeline/stage3_static/static_validator.py`
- `_check_intra_conflicts(flows)` 함수 신규 추가:
  - 동일 디바이스의 같은 트래픽에 상반된 액션 감지 (Intra-Shadowing)
  - catch-all 룰이 특정 프로토콜 룰을 가리는 경우 감지
- `validate()` Step 3: `intent_action == "compound"`일 때 intra-conflict 검사 실행

#### `api.py`
- Stage 1: `prediction.compound` 분기 처리
- Stage 2: `compile_compound()` vs `compile_flowrule()` 분기

---

## 6. 문서 작성: Intent 유형 가이드

**`docs/INTENT_GUIDE.md`** 신규 작성

5가지 intent 유형(block, forward, qos, sfc, reroute) 설명:
- 각 유형의 네트워크 의미와 실제 사용 사례
- 영어 요청이 한국어보다 정확한 이유
- 필수/선택 필드 표
- 거절(rejection) 원인과 예시
- 영어 예시 + 한국어 번역

---

## 7. E1 실험 분석 (sdn_intent-framework)

### 데이터셋 구성

| 항목 | 내용 |
|---|---|
| 위치 | `sdn_intent-framework/experiments/e1/` |
| 케이스 수 | 100개 (N001~N050 upstream + 50개 project-authored) |
| 카테고리 | Forwarding 42 / Security 23 / QoS 23 / Compound 2 / Rejection 10 |
| Gold 상태 | Provisional (이중 annotation 미완료) |
| SFC/Reroute 확장 | 별도 100케이스 (`intents_sfc_reroute.jsonl`) |

### 4가지 실험 트리트먼트

| 트리트먼트 | 설명 |
|---|---|
| E1-A | LLM → ONOS FlowRule 직접 생성 (IR 없음) |
| E1-B | LLM → IR, zero-shot |
| E1-C | LLM → IR + few-shot examples |
| E1-D | LLM → IR + few-shot + topology grounding |

**실행 모델**: `qwen3:8b` (Ollama, temperature=0.2) × 5 repetition × 100 case = 2,000 calls

### 주요 결과 (e1_results.md)

| Metric | E1-A | E1-B | E1-C | **E1-D** |
|---|---:|---:|---:|---:|
| Schema validity | 0.432 | 0.830 | 0.930 | 0.910 |
| Exact match | 0.020 | 0.200 | 0.218 | **0.368** |
| Rule-count acc. | 0.013 | 0.778 | 0.844 | 0.822 |
| Hallucination rate | 0.036* | 0.159 | 0.241 | **0.021** |
| Rejection rate | 1.000* | 0.400 | 0.700 | 0.680 |

\* E1-A의 수치는 대량 과잉거절(over-rejection)의 부산물

**Upstream-only 통제 비교 (task-equivalence confound 제거, N=50)**

| Metric | E1-A | E1-B | E1-C | **E1-D** |
|---|---:|---:|---:|---:|
| Exact match | 0.000 | 0.000 | 0.000 | **0.280** |
| Hallucination | 0.050 | 0.289 | 0.486 | **0.042** |

### 핵심 발견

1. **IR 형식이 schema validity를 크게 높인다** — 43% → 83~93%
2. **Few-shot 단독은 hallucination을 오히려 악화시킨다** — 0.159 → 0.241 (B→C)
3. **Topology grounding이 hallucination을 실질적으로 억제한다** — 0.241 → 0.021 (C→D)
4. **Grounding 효과는 `device` 슬롯 하나에 집중된다** — device accuracy 0.481 → 0.924

### IR 스키마 차이

E1 framework와 현재 파이프라인의 IR 스키마가 다름:

| E1 framework | 파이프라인 `IntentIR` |
|---|---|
| `action: "deny"` | `action: "block"` |
| `action: "prioritize"` | `action: "qos"` |
| `selector.source.ip` | `src_ip` |
| `enforcement.device` | `device_hint` |
| `enforcement.egress_port` | `out_port` |
| `selector.source: null` 허용 | src_ip 미필수 케이스 다수 존재 |

---

## 8. 다음 할 일

- [ ] E1 실험을 파이프라인(Gemini)으로 재현
  - 데이터셋 변환 스크립트 작성 (E1 IR → 파이프라인 IR)
  - 실험 러너 작성 (T-B/C/D treatments)
  - 스코어러 작성
- [ ] Provisional gold → 독립 이중 annotation 완료
- [ ] Rule 순서 기반 매칭 → type+action 최적 매칭으로 개선
- [ ] SFC/Reroute 케이스 E1 확장 실험

---

## 9. UI 대규모 개편 (세션 2차)

### 9-1. 레이아웃 개편

**변경 내용**
- Stage 카드를 최상단 배치, 아래에 인텐트 입력창, 맨 아래에 Run Pipeline
- 좌우 배치 전환 버튼 추가 (`⇄`) — 사이드바는 항상 왼쪽 고정, main/topology 패널만 교체
- 전환 후 패널 리사이저 방향 자동 반전 (`layout-swapped` 클래스 감지)
- 기본 토폴로지 패널 너비 `50vw` (localStorage 미설정 시)
- `1초 리프레시` 뱃지 제거

**`static/index.html`** — `#stages-section` → `#decision-banner` → `#intent-section` 순서로 재배치, `#layout-swap-btn` 추가

**`static/style.css`** — CSS `order` 속성으로 좌우 전환 구현 (sidebar order:0 고정)

**`static/app.js`** — `toggleLayoutSwap()`, localStorage 저장

### 9-2. Intent 프리셋 드롭다운

기존 칩 목록 → 토폴로지 프리셋과 동일한 드롭다운 패턴으로 변경.

- `#intent-preset-btn` + `#intent-preset-menu.preset-menu` 구조
- `position: fixed` + `getBoundingClientRect()`로 overflow 없이 렌더링
- 그룹: Block / Forward / QoS·SFC·Reroute / Compound / Current Topology (동적)
- `initIntentPresets()` 함수 추가

### 9-3. 토폴로지 패널 개선

- 패널 헤더 클릭 시 토폴로지 전체 접기/펼치기 (`topo-collapsed` 토글)
- 하단 METRICS / HOST IP MAP / FLOW RULES 섹션을 독립 토글로 분리
  - `#topo-info-scroll` 래퍼 + `max-height` 스크롤 영역
  - 섹션 토글 시 `setTimeout(syncTopoSize, 260)` 호출로 SVG 크기 재계산
- 전체화면 버튼 제거

### 9-4. D3 줌 레이어 추가

토폴로지 SVG에 마우스 휠 줌/드래그 패닝 추가.

```javascript
topoZoomLayer = topoSvg.append('g').attr('class', 'zoom-layer');
topoZoom = d3.zoom().scaleExtent([0.2, 4])
  .on('zoom', ev => topoZoomLayer.attr('transform', ev.transform));
topoSvg.call(topoZoom);
// 더블클릭: 줌 리셋
topoSvg.on('dblclick.zoom', () =>
  topoSvg.transition().duration(300).call(topoZoom.transform, d3.zoomIdentity));
```

- 에디터 모드에도 동일 적용
- 에디터 클릭/mousemove 좌표를 `d3.zoomTransform().invert()`로 변환

### 9-5. Stage 카드 점진적 등장

파이프라인 실행 전에는 스테이지 카드 미표시. 각 스테이지가 시작될 때 카드가 슬라이드-인 애니메이션으로 하나씩 등장.

```javascript
function ensureStageCard(stageNum) {
  if (document.getElementById(`stage-${stageNum}`)) return;
  // 카드 생성 + .stage-card-enter 클래스 → requestAnimationFrame → 클래스 제거
}
```

**`static/style.css`** — `.stage-card-enter { opacity:0; transform:translateY(10px); }`

### 9-6. 패널 리사이저 버그 수정

**증상**: 토폴로지 패널을 늘려도 D3 노드들이 왼쪽에 뭉쳐 있음.

**원인**: D3 tick 콜백이 초기 `w`/`h`를 클로저로 캡처해서 컨테이너가 커져도 경계 계산에 반영 안 됨.

**수정** (`static/app.js`):
```javascript
simulation.on('tick', () => {
  const topoEl = document.getElementById('topology-graph');
  const cw = topoEl ? topoEl.clientWidth  : w;
  const ch = topoEl ? topoEl.clientHeight : h;
  // cw/ch 기준으로 노드 clamping
});
```

`syncTopoSize()` 함수가 SVG viewBox, D3 `forceCenter`, 시뮬레이션 재시작을 모두 처리.

---

## 10. 복합 인텐트(Compound) 전 파이프라인 지원

### 10-1. Stage 3 — 정적 검증: 포트 인식 충돌 탐지

**버그**: "Allow HTTP port 80 + Block SSH port 22" 복합 인텐트에서 `Intra-Shadowing` 오탐 발생.

**원인** (`pipeline/stage3_static/static_validator.py`): `_check_intra_conflicts()`가 `ETH_TYPE + IPV4_SRC + IPV4_DST + IP_PROTO` 4가지가 일치하면 충돌로 판정했는데, TCP_DST/UDP_DST 포트가 다른 경우(HTTP=80, SSH=22)를 다른 트래픽으로 인식하지 않음.

**수정**:
```python
port_types = {"TCP_DST", "UDP_DST", "TCP_SRC", "UDP_SRC"}
if key_types.issubset(shared_types):
    match = all(c1[t] == c2[t] for t in key_types ...)
    if match:
        for pt in port_types:
            if pt in c1 and pt in c2 and c1[pt] != c2[pt]:
                match = False  # 포트가 다르면 다른 트래픽 → 충돌 아님
                break
```

### 10-2. Stage 4 — Digital Twin: 복합 인텐트 전 sub-rule 검증

**버그**: "Forward H1→H3 + Block H8→H4" 복합 인텐트에서 forward만 검증하고 block은 확인 안 함.

**원인** (`pipeline/stage4_twin/twin_verifier.py`): `verify()` 함수가 단일 `(action, src_ip, dst_ip, ...)` 튜플만 사용.

**수정**: `intent_specs` 리스트로 전환 후 모든 sub-rule 순회:

```python
intent_specs = []
for sr in sub_rules:
    # ... criteria 파싱
    intent_specs.append((sr_action, sr_src, sr_dst, sr_proto, sr_port, sr_flow))

for spec_idx, (spec_action, spec_src_ip, spec_dst_ip, ...) in enumerate(intent_specs):
    check_key = "intent_check" if len(intent_specs) == 1 else f"intent_check_{spec_idx}"
    # block: 스티어링 룰 + 차단 확인
    # forward: 도달 확인
```

결과 예시 (복합 인텐트):
```json
{
  "checks": {
    "baseline_connectivity": true,
    "intent_check_0": true,
    "intent_check_1": true,
    "regression": true
  }
}
```

### 10-3. Stage 5 — XAI: compound ir=None 처리

**버그**: 복합 인텐트에서 `ir=None`이어서 `'NoneType' object has no attribute 'action'` 에러.

**수정** (`pipeline/stage5_xai/explainer.py`):
- `explain()` 시그니처에 `compound: Optional[CompoundIntentIR] = None` 추가
- `effective_ir = ir if ir is not None else (compound.rules[0] if compound else None)`
- `_summarize_ir()`: compound일 때 모든 rule 요약 출력
- `_llm_decision_reason()`: `ir_desc = f"action={ir.action}..." if ir else "(복합 인텐트)"`

**`api.py`**: `XAIExplainer.explain(..., compound=compound)` 전달

### 10-4. UI — 복합 인텐트 토폴로지 오버레이

**버그**: compound 인텐트에서 첫 번째 sub-rule 오버레이(SRC/DST)만 표시되고, 두 번째 sub-rule(block)의 BLOCK 오버레이가 안 나타남.

**원인 1** (`api.py`): `twin_info` 이벤트를 `flowrule["flows"][0]` 기준으로 한 번만 전송. compound sub-rules를 순회하지 않음.

**수정**: sub_rules 순회 후 각각 `twin_info` 이벤트 전송:
```python
_sub_rules = flowrule.get("sub_rules", []) if _is_compound else [flowrule]
for sr in _sub_rules:
    _emit_twin_info(sr)
```

**원인 2** (`static/app.js`): `twinVizInfo` 단일 객체 → 새 이벤트가 올 때마다 덮어씀.

**수정**: `twinVizInfo` → `twinVizInfoList[]` 배열 전환:
```javascript
// onTwinInfo: push instead of overwrite
twinVizInfoList.push({ srcNode, dstNode, blockNode, action: ev.action });

// renderTwinHighlights: iterate all entries, deduplicate by node id
for (const { srcNode, dstNode, blockNode, action } of twinVizInfoList) { ... }

// setTwinPhase('intent'): animate each sub-rule
for (const { srcNode: sn, dstNode: dn, blockNode: bn, action } of twinVizInfoList) {
  if (action === 'block' && sn && bn)  startPacketLoop(path, '#f87171', stopIdx);
  else if (action !== 'block' && sn && dn) startPacketLoop(path, '#10b981', undefined);
}
```

**원인 3** (`static/app.js`): `setTwinPhase` 트리거 조건이 `'[intent]'` 포함 여부를 검사하는데, compound 인텐트는 `'[intent_0]'`, `'[intent_1]'`으로 로그가 찍혀 매칭 실패.

**수정**:
```javascript
// 변경 전
m.includes('[intent]')
// 변경 후
m.includes('[intent')   // compound suffix([intent_0], [intent_1]) 포함
```

---

## 11. SFC 인텐트 제약 사항 정리

SFC(Service Function Chaining) 인텐트는 컴파일러 구조상 두 개의 FlowRule이 필요:

| Rule | 역할 | 필요 필드 |
|---|---|---|
| ingress | 매칭 트래픽 → waypoint 포트 전송 | `out_port` (waypoint 포트 번호) |
| egress | waypoint 복귀 → 목적지 전송 | `alt_out_port` (복귀 후 출력 포트) |

`alt_out_port` 미지정 시 `CompileError` 발생. 물리적 토폴로지를 알아야 지정 가능하므로, LLM이 자동 추론하기 어려운 필드.

**권장 intent 작성법**:
```
h1에서 h4로 가는 트래픽을 s1 포트 9의 IDS를 거쳐 포트 2로 나오게 해줘
→ out_port=9, alt_out_port=2 추론 가능
```

단순 트래픽 확인/모니터링 목적이라면 `forward` 인텐트 사용 권장.
